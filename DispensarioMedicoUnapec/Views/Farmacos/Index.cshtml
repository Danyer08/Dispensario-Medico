@model IEnumerable<DispensarioMedicoUnapec.Farmacos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p style="float:right">
    @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-primary" })
</p>
<form method="get" style="margin-bottom:15px">
    <input style="float:left" class="form-control" type="text" id="Criterio" name="Criterio" />
    <input class="btn btn-outline-primary" type="submit" id="cdmBuscar" value="Buscar" />
</form>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PrincipioActivo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FormaFarmaceutica)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ViaAdmin)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PrincipioActivo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FormaFarmaceutica)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ViaAdmin.Nombre)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.IdFarmaco }) |
                @Html.ActionLink("Details", "Details", new { id = item.IdFarmaco }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.IdFarmaco })
            </td>
        </tr>
    }

</table>
